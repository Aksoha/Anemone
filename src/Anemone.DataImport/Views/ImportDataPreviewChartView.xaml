<UserControl x:Class="Anemone.DataImport.Views.ImportDataPreviewChartView" x:Name="PreviewChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wpf="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Text="{Binding Title, ElementName=PreviewChart}"
                   HorizontalAlignment="Center"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}" />
        <wpf:CartesianChart Grid.Row="1"
                            Series="{Binding Series, ElementName=PreviewChart}"
                            XAxes="{Binding XAxes, ElementName=PreviewChart}"
                            YAxes="{Binding YAxes, ElementName=PreviewChart}"
                            TooltipPosition="Hidden" />
    </Grid>
</UserControl>